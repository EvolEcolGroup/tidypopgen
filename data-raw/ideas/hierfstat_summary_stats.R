# hierfstat notes
fis.dosage(dos_hier_match,matching=TRUE, pop=test_indiv_meta$population)

fst.dosage(dos_hier_match,matching=TRUE, pop=test_indiv_meta$population)

# these return a vector of n populations plus a global value
# write a function that returns the n vector, and add a parameter that allows the inclusion
# of the global value


## For Hudson Fst, we could check against:
# https://www.rdocumentation.org/packages/KRIS/versions/1.1.6/topics/fst.hudson
# also have a look at fst4pg
